<h3>All Users</h3>
<table border="0">
<tr>
<th>Name</th>
<th>Email</th>
</tr>
<% @users.each do |user| %>
  <tr>
  <td><%= user.firstname %> <%= user.lastname %></td>
  <td><%= link_to user.email, admin_user_path(user) %></td>
  </tr>
<% end %>
</table>
<br>
<%= paginate @users %>
<br>
